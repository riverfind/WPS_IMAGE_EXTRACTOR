# WPS Image Extractor for Tkinter

## 1. 项目目标

开发一个基于 Python Tkinter 的桌面 GUI 程序，用于从 WPS 文档中提取、浏览、筛选、删除、过滤与导出图片，重点解决文档内重复图片识别、批量筛选以及回溯定位问题。

该工具应优先面向本地单机使用场景，强调以下能力：

- 从 WPS 文档中读取全部图片及其位置信息。
- 以缩略图网格方式高效浏览大量图片。
- 支持单图预览、双击定位到文档中的原始位置。
- 支持基于 MD5 的相同图片快速批量点选。
- 支持批量删除、批量过滤、批量导出。
- 所有高风险操作具备明确确认与可理解反馈。

## 2. 目标用户

- 需要清理 WPS 文档冗余图片的办公人员。
- 需要提取或归档文档图片的资料整理人员。
- 需要快速发现重复图片并进行人工确认的技术支持人员。

## 3. 产品范围

### 3.1 本期范围

- 打开单个 WPS 文档。
- 解析并提取文档中的全部图片。
- 展示图片缩略图、基础元数据与选中状态。
- 单击缩略图查看大图预览。
- 双击缩略图跳转到文档中对应图片位置。
- 一键选中相同图片（按 MD5 分组）。
- 删除选中图片，删除前二次确认。
- 批量过滤选中图片，使其从当前缩略图视图中隐去。
- 导出选中图片到用户指定目录。
- 提供全选、反选、搜索、状态统计等基础交互。

### 3.2 暂不纳入本期

- OCR、图像增强、压缩、去水印等图像处理能力。
- 多文档同时打开与对比。
- 云端同步、协同编辑、账号体系。
- 完整撤销/重做历史。
- 针对所有 WPS 私有格式的深度兼容保证。

## 4. 核心用户故事

1. 作为用户，我希望打开一个 WPS 文档后能看到全部图片缩略图，以便快速浏览内容。
2. 作为用户，我希望单击缩略图时右侧显示放大预览，以便确认细节。
3. 作为用户，我希望双击缩略图时能定位到文档中的图片位置，以便回到上下文判断是否保留。
4. 作为用户，我希望点击“按 MD5 选相同图”后，能一次性选中与当前图片相同的所有项，以便处理重复图片。
5. 作为用户，我希望删除图片前看到二次确认和缩略图清单，以避免误删。
6. 作为用户，我希望将选中图片临时过滤出视图，以便聚焦剩余图片继续判断。
7. 作为用户，我希望把选中的图片导出到指定目录，以便外部归档或复用。

## 5. 功能需求

### 5.1 文档导入

- 支持用户通过文件选择器打开 WPS 文档。
- 首期优先支持可解包/可解析的 Office Open XML 兼容格式，如 `.docx`。
- 对 `.wps` 等格式，若无法直接解析，需提供明确错误提示或适配器降级策略。
- 导入完成后展示：
  - 文档名称
  - 图片总数
  - 重复组数量
  - 当前已选数量
  - 当前已过滤数量

### 5.2 缩略图浏览

- 主区域采用可滚动缩略图网格。
- 每个缩略图卡片至少包含：
  - 缩略图
  - 图片文件名或内部编号
  - 分辨率
  - 文件大小
  - 选中状态
- 缩略图应支持鼠标单击、双击、右键菜单。
- 大量图片场景下应避免一次性渲染全部原图，需采用缩略图缓存与惰性刷新策略。

### 5.3 预览与定位

- 单击缩略图：仅更新右侧预览区，不改变勾选状态。
- 双击缩略图：调用文档定位能力，跳转到该图片所在文档位置。
- 若当前环境无法直接驱动 WPS 跳转，应显示“暂不支持定位”或给出替代说明，不可无反馈失败。

### 5.4 选择能力

- 支持全选。
- 支持反选。
- 支持通过复选框逐项点选。
- 支持按 MD5 选中与当前图片相同的全部图片。
- 支持通过搜索框按文件名、MD5、分组信息快速过滤视图。

### 5.5 删除能力

- 可删除当前选中图片。
- 删除前必须弹出二次确认窗口。
- 确认窗口中需展示待删除图片缩略图列表。
- 确认窗口中的缩略图应与主界面保持一致的核心交互语义：
  - 可单击切换预览
  - 可双击定位到文档位置
  - 不在确认弹窗内扩展删除范围
- 删除操作默认不可撤销，文案必须明确提示。
- 删除功能仅处理当前明确勾选的图片；若需要扩展到相同图片，应先在主界面使用“按 MD5 选相同图”完成批量勾选。

### 5.6 过滤能力

- 支持批量过滤选中图片。
- 过滤后的图片从当前缩略图视图中隐去，但不修改原文档内容。
- 应提供状态统计，让用户知道已过滤数量。
- 过滤属于视图层状态，后续应预留“恢复过滤项”能力的扩展位。

### 5.7 导出能力

- 支持将选中图片导出到指定目录。
- 若出现重名文件，需定义冲突策略：
  - 自动重命名为默认策略。
- 导出完成后给出成功/失败统计。
- 对已过滤但仍处于选中状态的图片，需在设计上定义是否允许导出；默认建议允许，只要仍属于当前数据集。

## 6. 关键交互原则

- 高风险操作先确认，后执行。
- 所有批量操作都需要清晰反馈数量。
- 任何“定位失败”“删除失败”“导出失败”都必须给出原因或降级提示。
- 主界面优先服务“快速筛选与去重”，减少多层弹窗。
- 缩略图交互语义在不同界面尽量保持一致，降低学习成本。

## 7. 非功能需求

### 7.1 性能

- 在 200-500 张图片场景下保持可用，不因一次性加载原图导致明显卡死。
- 缩略图生成应使用后台任务或分批渲染。
- MD5 计算应支持进度反馈或延迟计算策略。

### 7.2 可靠性

- 文档解析失败时不应导致程序崩溃。
- 删除前需生成明确的待处理清单。
- 导出时单文件失败不应中断整个批次。

### 7.3 可维护性

- 采用分层设计：GUI、应用服务、领域模型、文档适配器、基础设施解耦。
- 图片数据对象、选中状态、过滤状态、重复分组需统一建模。
- 所有与 WPS/文档格式耦合的逻辑收敛在适配器层。

## 8. 建议技术方案

- GUI：`tkinter` + `ttk`
- 图片处理：`Pillow`
- 文档解析：
  - `zipfile` 用于 OOXML 容器读取
  - `xml.etree.ElementTree` 或 `lxml` 解析文档关系与位置信息
- 数据摘要：`hashlib.md5`
- 文件操作：`pathlib`, `shutil`
- 并发建议：`threading` + `queue`，主线程只负责 UI 更新

## 9. 开发环境约定

### 9.1 环境管理

- 本项目使用 `uv` 管理 Python 环境与依赖。
- 默认通过 `uv` 维护虚拟环境，不使用手动 `venv` 工作流。
- 所有面向项目的运行命令应优先写成 `uv` 形式，便于团队统一执行方式。

### 9.2 启动方式

- 启动应用时应使用 `uv run`。
- 当前桌面 GUI 启动命令约定为：`uv run python main.py`
- 若后续引入模块化入口，也应保持 `uv run` 调用方式，例如：`uv run python -m app`

### 9.3 依赖管理

- 运行时依赖应使用 `uv` 管理。
- 新增项目依赖时，优先使用 `uv add <package>`
- 锁定或同步依赖时，遵循 `uv` 的项目级依赖文件更新机制。
- 不直接手工编辑依赖列表作为首选方式，除非是批量整理后的结果同步。

### 9.4 开发依赖管理

- 开发依赖按当前项目约定使用 `uv pip install` 安装。
- 例如：
  - `uv pip install pytest`
  - `uv pip install ruff`
  - `uv pip install black`
- 若后续团队决定统一改为 `uv add --dev`，应整体更新本文档后再切换，不应混用两套约定。

### 9.5 命令书写规范

- 文档、脚本、开发说明中的命令优先使用 `uv` 前缀。
- 示例：
  - 运行应用：`uv run python main.py`
  - 添加依赖：`uv add pillow`
  - 安装开发依赖：`uv pip install pytest`
- 后续新增 `README`、测试说明、发布说明时，需与本节保持一致。

## 10. 风险与约束

- “WPS 文档”并不等同于统一格式，`.docx` 与 `.wps` 的解析方式可能完全不同。
- “双击定位”能力强依赖文档内部位置信息是否可映射，以及本机是否可控制 WPS 打开指定位置。
- 删除图片如果要真正写回文档，会涉及文档结构重写与关系维护，属于高风险实现点。
- 对完全相同图片按 MD5 分组时，只能识别二进制完全一致，不等价于“视觉相似”。

## 11. 推荐实施阶段

### 阶段 1：可浏览版本

- 支持打开 `.docx`
- 提取图片
- 缩略图展示
- 右侧预览
- 全选/反选
- 搜索
- MD5 分组

### 阶段 2：批量处理版本

- 批量过滤
- 批量导出
- 删除确认弹窗
- 删除写回文档

### 阶段 3：增强版本

- 更强的 WPS 定位能力
- 过滤恢复面板
- 更完整的错误恢复与任务进度
- 多格式适配器扩展

## 12. 验收标准

- 能成功打开一个受支持文档并展示全部图片缩略图。
- 单击图片时右侧能显示对应预览。
- 双击图片时能触发定位流程，并在失败时给出明确提示。
- 点击“按 MD5 选相同图”后，当前图片所属重复组会被整体选中。
- 删除时必须弹出二次确认，且显示待删除缩略图。
- 过滤操作后，所选图片从当前视图中隐去。
- 导出操作后，选中图片成功输出到指定目录，并给出统计结果。

## 13. 文档导航

- 宏观架构：`docs/architecture-overview.md`
- 微观架构（GUI）：`docs/microarchitecture-gui.md`
- 微观架构（核心服务）：`docs/microarchitecture-core.md`
- 微观架构（文档适配器）：`docs/microarchitecture-document-adapters.md`
- GUI 核对图：`docs/gui-mockup-review.md`
